This is an attempted version of chess in the terminal where pieces 
are not in bitboards but instead all stored in a single table (bad approach)
Currently working on a better version using bitboards
-ader
